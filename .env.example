# Example environment configuration for Supply Chain Game
# Copy this file to `.env.local` and fill in real values for your environment

# --- Database ---
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/supply_chain_game # Main PostgreSQL connection string (used by app, Prisma, and scripts)

# --- Authentication ---
NEXTAUTH_SECRET=your-very-secret-key # Used for session encryption (required in production)

# --- Deployment/Environment ---
NODE_ENV=development # Set to 'production' in production deployments

# --- Debugging (optional) ---
DEBUG_SQL=false # Set to 'true' to log SQL queries in development

# --- Local DB Scripts (optional, only for local setup scripts) ---
DB_USER=postgres # Only used by setup scripts if DATABASE_URL is not set
DB_PASSWORD=postgres # Only used by setup scripts if DATABASE_URL is not set
